import 'package:flutter/material.dart';

class TabBodyShow extends StatelessWidget {
  const TabBodyShow({
    super.key,
  });

  static const tabTitle = "Show";

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Show Tab'));
  }
}
