import 'package:flutter/material.dart';

class TabBodySetlists extends StatelessWidget {
  const TabBodySetlists({
    super.key,
  });

  static const tabTitle = "Setlists";

@override
  Widget build(BuildContext context) {
    return Center(child: Text('Setlists Tab'));
  }
}
