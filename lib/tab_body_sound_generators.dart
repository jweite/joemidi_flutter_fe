import 'package:flutter/material.dart';

class TabBodySoundGenerators extends StatelessWidget {
  const TabBodySoundGenerators({
    super.key,
  });

  static const tabTitle = "Sound Gens";

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Sound Generators Tab'));
  }
}
