import 'package:flutter/material.dart';

class TabBodySongs extends StatelessWidget {
  const TabBodySongs({
    super.key,
  });

  static const tabTitle = "Songs";

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Songs Tab'));
  }
}
