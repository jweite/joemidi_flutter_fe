import 'package:flutter/material.dart';

class TabBodyMappings extends StatelessWidget {
  const TabBodyMappings({
    super.key,
  });

  static const tabTitle = "Mappings";

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Mappings Tab'));
  }
}
