import 'package:flutter/material.dart';

class TabBodyMisc extends StatelessWidget {
  const TabBodyMisc({
    super.key,
  });

  static const tabTitle = "Misc";

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Misc Tab'));
  }
}
